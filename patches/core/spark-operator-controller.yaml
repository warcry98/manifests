apiVersion: apps/v1
kind: Deployment
metadata:
  name: spark-operator-controller
spec:
  template:
    spec:
      containers:
      - name: spark-operator-controller
        resources:
          requests:
            cpu: 200m
            memory: 409Mi
          limits:
            cpu: 400m
            memory: 819Mi
